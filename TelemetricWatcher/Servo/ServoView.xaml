<UserControl x:Class="TelemetricWatcher.Servo.ServoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelemetricWatcher.Servo"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="50"
             d:DesignWidth="150"
             d:DataContext="{d:DesignInstance Type=local:ServoViewModel}">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding Id}"/>
        <TextBlock Text="{Binding Temperature, StringFormat=Temperature {0}°C}"/>
        <TextBlock Text="{Binding Voltage, StringFormat=Voltage {0}V}"/>
        <TextBlock Text="{Binding Load, StringFormat=Load {0}%}"/>
    </StackPanel>
</UserControl>
